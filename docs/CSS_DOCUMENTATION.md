# CSS 样式表详细文档

本文档为您详细解释项目中的三个主要 CSS 文件的功能和应用。

---

## 📄 文件结构概览

```
static/css/
├── styles.css          ← Bootstrap 基础样式（外部库，通常不需修改）
├── main.css            ← 全站通用样式（导航栏、布局、排版等）
├── home.css            ← 首页专用样式（个人资料、新闻等）
├── detail-pages.css    ← 详情页通用样式
├── footer-unified.css  ← 底部统一样式
└── [其他页面特定样式]
```

---

## 🎯 main.css - 全站通用样式

### 文件位置
`static/css/main.css`

### 主要功能
控制整个网站的全局样式，包括：
- 导航栏外观和交互
- 页面头部和底部
- 排版和颜色
- 邮件复制功能UI
- 响应式布局

### 详细模块说明

#### 1. 全局变量 (`:root`)
```css
:root {
    --h-title-color: #24306b;  /* 深蓝色主色，用于所有标题和导航栏 */
}
```
**应用于：** 所有使用 `var(--h-title-color)` 的地方

---

#### 2. 导航栏 (`.header`)
```css
.header {
    border-bottom: solid 2px var(--bs-blue);  /* 导航栏下方蓝线 */
    z-index: 10000;  /* 最高层级 */
}
```
**应用于：** 所有页面顶部的 `<nav class="header">`

**页面效果：** 蓝色分割线，导航栏始终浮在内容之上

---

#### 3. 页面底部 (`.bg-bottom`)
```css
.bg-bottom {
    background-color: var(--h-title-color);  /* 深蓝色背景 */
}
```
**应用于：** 所有页面的 `<footer class="bg-bottom">`

**页面效果：** 底部footer为深蓝色背景

---

#### 4. 顶部内容区块 (`.top-section`)
用于各页面的标题区域（如 research.html、events.html 的大标题背景区）

**关键属性：**
- `position: relative` - 为伪元素服务
- `padding: 10rem 0` - 上下大内边距
- `height: 25rem` - 固定高度
- `:before` 伪元素 - 添加半透明黑色遮罩 (rgba(0,0,0,0.5))

**应用于：**
- research.html 的 "Research" 标题区
- events.html 的 "Events" 标题区
- teaching.html、blog.html 等的标题背景

---

#### 5. 各页面标题 (`section header h2`)
```css
section header h2 {
    font-family: "Kanit", "Cambria", "Mulish", sans-serif;
    color: var(--h-title-color);  /* 深蓝色 */
    font-size: 2.5rem;            /* 大标题 */
    line-height: 3rem;
    font-weight: 800;             /* 超粗 */
    margin-bottom: 1.5rem;
}
```

**应用于：**
- 所有页面的页头标题（Research、Events、Teaching 等）

**效果：** 大的、粗的、深蓝色标题

---

#### 6. 页面正文 (`section .main-body`)
```css
section .main-body {
    font-size: 1.3rem;   /* 较大字号，易于阅读 */
    font-weight: 400;    /* 常规粗细 */
    line-height: 2rem;   /* 宽松行高 */
}
```

**应用于：** 各页面的主要内容区域

---

#### 7. 背景渐变

**浅色渐变** (`.bg-gradient-primary-to-secondary-light`)
```css
background: linear-gradient(45deg, #fcfcfc, #ffffff);
```
应用于：index.html 主内容区（近乎白色的浅渐变）

**灰色渐变** (`.bg-gradient-primary-to-secondary-gray`)
```css
background: linear-gradient(45deg, #f0f0f0, #f1f1f1);
```
应用于：备选背景方案

---

#### 8. 个人头像 (`#avatar`)
用于 about.html 页面

**关键属性：**
- `position: relative` + `top: min(-65px, ...)` - 向上拉，与标题重叠
- `float: right` - 浮动到右侧
- 响应式高度：`height: max(130px, min(200px, calc(100vw/6)))`
- `box-shadow` - 投影效果

**应用于：** about.html 的个人头像

---

#### 9. 邮件功能 (`.email-*`)

**容器** (`.email-container`)
- `display: inline-flex` - Flexbox 布局
- `gap: 0.6rem` - 图标和文字间距

**图标** (`.email-icon`)
- 默认：灰色，光标为手型
- 悬停：放大115%，透明度降低

**文字** (`.email-text`)
- `font-size: 1.2rem` - 较大字号
- `color: var(--h-title-color)` - 深蓝色

**复制反馈** (`.email-text.copied::after`)
- 显示绿色的 "✔ Copied" 提示

**应用于：** about.html 的联系方式部分

---

#### 10. 导航栏右侧功能区

**容器** (`#navbar-right-section`)
```css
margin-left: auto;  /* 关键：使其靠右 */
align-items: center;
gap: 0.5rem;
```
包含：搜索框、语言切换、社交图标

**搜索框** (`#nav-search-input`)
- `width: 120px` - 宽度
- `font-size: 0.85rem` - 小字号

**语言切换** (`.lang-switch button`)
- 默认：灰色文字，无背景
- `.active` 状态：深灰色背景，白色文字

**社交图标** (`.nav-social-links .social-icon i`)
- 默认：灰色 (#7b7c7d)
- 悬停：更深的灰色 (#858587)

**应用于：** 所有页面的导航栏右侧

---

#### 11. 导航栏链接高亮 (`.navbar-nav .nav-link.active`)

**当用户访问某个页面时，对应的导航链接会自动高亮：**

```css
.navbar-nav .nav-link.active {
    color: var(--bs-blue) !important;  /* 蓝色文字 */
    font-weight: 600;                  /* 加粗 */
    border-bottom: 2px solid var(--bs-blue);  /* 蓝色底线 */
    padding-bottom: 0.3rem;
}
```

**工作原理：**
- navbar.js 会自动检测当前页面
- 为对应的链接添加 `active` class
- detail 页面也会高亮对应的列表页链接（如 research-detail.html → 高亮 RESEARCH）

**应用于：** 所有页面的导航栏

---

#### 12. 响应式设计

```css
@media (max-width: 991px) {
    #navbar-right-section {
        flex-direction: column;  /* 改为垂直排列 */
        align-items: flex-start;
        gap: 0.3rem;
    }
    #nav-search-input {
        width: 100px;  /* 缩小搜索框 */
    }
}
```

**应用于：** 平板和手机设备（屏幕宽度 ≤ 991px）

---

---

## 🏠 home.css - 首页专用样式

### 文件位置
`static/css/home.css`

### 主要功能
控制首页（index.html）的样式

### 详细模块说明

#### 1. 基础布局
```css
#home-profile, #home-biography, #home-news {
    text-align: center;
    margin-bottom: 1rem;
}
```
**应用于：** 首页的三个主要区域（个人资料、简介、新闻）

---

#### 2. 个人头像
```css
#home-profile img {
    border-radius: 50%;  /* 圆形 */
    box-shadow: 0px 4px 10px rgba(0,0,0,0.2);  /* 下阴影 */
}
```
**应用于：** 首页的个人照片

**效果：** 圆形头像，带阴影

---

#### 3. 个人名字
```css
#home-profile h2 {
    font-family: Kanit, "Cambria", "Mulish", sans-serif;
    font-size: 2rem;
    font-weight: 700;
    margin-top: 0.5rem;
}
```
**应用于：** 首页的个人名字标题（"Xuefeng Xu"）

**效果：** 大、加粗、深蓝色

---

#### 4. 社交链接按钮
```css
.social-link {
    display: inline-flex;
    align-items: center;
    padding: 0.05rem 0.5rem;
    border-radius: 12px;
    border: 2px solid #24306b;  /* 深蓝色边框 */
    background-color: #ffffff;  /* 白色背景 */
    color: #24306b;
    transition: all 0.2s ease;
}

.social-link:hover {
    background-color: #e0e0e0;  /* 灰色背景 */
    transform: translateY(-2px);  /* 向上浮起 */
}
```
**应用于：** 首页的社交链接（GitHub、Google Scholar）

**效果：** 白色背景 + 深蓝色边框的按钮，悬停时背景变灰、上浮

---

#### 5. 分割线
```css
.profile-divider {
    width: 40%;
    height: 1.5px;
    background-color: #d3d3d3;  /* 浅灰色 */
    margin: 0.5rem auto;
    border-radius: 1px;
}
```
**应用于：** 个人名字下方的装饰横线

---

#### 6. About Me 部分
```css
#home-biography h3 {
    font-family: Kanit, "Cambria", "Mulish", sans-serif;
    font-size: 1.5rem;
    color: #24306b;
}

#aboutme-md {
    font-size: 1rem;
    line-height: 1.5rem;
    text-align: justify;  /* 两端对齐 */
}
```
**应用于：** 首页左列的 "About Me" 部分

---

#### 7. 新闻列表

**列表容器** (`.news-list`)
```css
.news-list {
    list-style: none;
    padding-left: 0;
    margin: 0;
}
```

**单条新闻** (`.news-list li`)
- `white-space: nowrap` - 单行显示
- `text-overflow: ellipsis` - 超长时显示"..."
- 悬停时自动水平滚动显示完整内容

**新闻日期** (`.news-date`)
```css
.news-date {
    font-weight: bold;
    color: #24306b;  /* 深蓝色 */
}
```

**新闻链接** (`.news-link`)
```css
.news-link {
    color: #3948d2;  /* 蓝色 */
    text-decoration: none;
    font-weight: 600;
}
```

**应用于：** 首页右列的新闻列表

**特殊效果：** 超长标题自动滚动显示全文

---

#### 8. 新闻滚动动画
```css
@keyframes li-scroll {
    0% {
        transform: translateX(0);
    }
    100% {
        transform: translateX(-50%);
    }
}
```
**触发条件：** 用户悬停在超长新闻标题上

**效果：** 文字从右向左缓慢滑动，显示隐藏部分

---

#### 9. "更多新闻"链接
```css
.news-more-link {
    display: inline-block;
    margin-left: auto;  /* 靠右 */
    font-size: 1rem;
    font-weight: bold;
    color: #3948d2;  /* 蓝色 */
}
```
**应用于：** 首页新闻区域的 "More" 链接

**功能：** 指向完整新闻页面

---

---

## 📐 styles.css - Bootstrap 基础样式

### 文件位置
`static/css/styles.css`

### 说明
这是 Bootstrap 框架的 CSS 文件，包含了所有 Bootstrap 组件的基础样式。

### 主要包含内容
- 网格系统 (`.container`, `.row`, `.col-*`)
- 排版样式 (`h1-h6`, `p`, 等)
- 按钮 (`.btn`, `.btn-primary`, 等)
- 导航栏 (`.navbar`, `.nav`, 等)
- 卡片 (`.card`)
- 表单 (`.form-control`, 等)
- 工具类 (`.text-center`, `.mt-3`, 等)

### 通常情况
- **不需要修改**（除非要深度定制 Bootstrap）
- 被 main.css 和其他样式表覆盖

### 在项目中的角色
为整个网站提供基础的响应式布局和组件库。

---

---

## 📊 样式表应用关系图

```
styles.css (Bootstrap 基础)
    ↓
main.css (全站通用)
    ├→ 应用于所有页面的导航栏、底部、排版
    └→ 被其他特定样式表覆盖或补充

    ├→ home.css (首页专用)
    │  └→ 覆盖 main.css，添加首页特定样式
    │
    ├→ detail-pages.css (详情页通用)
    │  └→ 覆盖 main.css，添加详情页特定样式
    │
    └→ footer-unified.css (底部统一)
       └→ 覆盖 footer 相关样式
```

---

---

## 🎨 颜色系统

| 用途 | 颜色值 | 应用 |
|------|-------|------|
| 主色（深蓝） | #24306b | 标题、导航栏、底部背景 |
| 辅色（亮蓝） | #3948d2 | 链接、高亮 |
| Bootstrap蓝 | var(--bs-blue) | 导航栏高亮、分隔线 |
| 灰色（导航） | #7b7c7d | 社交图标、语言按钮 |
| 浅灰 | #d3d3d3 | 分割线 |
| 绿色 | #2e7d32 | 复制成功提示 |

---

---

## 🔧 修改建议

### 如果您想修改...

1. **导航栏颜色**
   - 修改 `main.css` 中的 `.header` 和 `--h-title-color`

2. **标题样式**
   - 修改 `main.css` 中的 `section header h2`

3. **首页个人资料部分**
   - 修改 `home.css` 中的 `#home-profile` 相关样式

4. **新闻列表效果**
   - 修改 `home.css` 中的 `.news-list` 相关样式和 `@keyframes li-scroll`

5. **响应式布局**
   - 修改各 CSS 文件中的 `@media` 查询

6. **全站主色**
   - 修改 `main.css` 中的 `:root { --h-title-color: #... }`

---

希望这份文档能帮助您更好地理解项目的 CSS 结构！
