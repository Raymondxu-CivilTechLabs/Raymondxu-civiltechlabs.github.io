# 系统架构与流程图

## 🏗️ 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     个人学术网页系统                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              导航栏 (Navbar)                              │   │
│  │  HOME | ABOUT | RESEARCH | BLOG | EVENTS | TEACHING | ...│   │
│  └──────────────────────────────────────────────────────────┘   │
│                          ↓                                       │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │           列表页面 (List Page)                            │   │
│  │   - events.html      (显示所有事件列表)                   │   │
│  │   - teaching.html    (显示所有教学内容)                   │   │
│  │   - interests.html   (显示所有兴趣)                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                          ↓ (点击条目)                           │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │          详情页面 (Detail Page)                           │   │
│  │   - events-detail.html?id=events1                         │   │
│  │   - teaching-detail.html?id=teaching1                     │   │
│  │   - interests-detail.html?id=interests1                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                          ↓                                       │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │        ContentManager 引擎 (content-manager.js)           │   │
│  │   - 自动扫描 markdown 文件                                │   │
│  │   - 解析 YAML front matter                                │   │
│  │   - 使用 marked.js 渲染 Markdown                          │   │
│  │   - 使用 MathJax 渲染数学公式                             │   │
│  └──────────────────────────────────────────────────────────┘   │
│                          ↓                                       │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │            内容源文件 (Markdown)                          │   │
│  │                                                            │   │
│  │   contents/                                                │   │
│  │   ├── events/                                              │   │
│  │   │   ├── events1.md                                       │   │
│  │   │   ├── events2.md                                       │   │
│  │   │   └── config.yml                                       │   │
│  │   ├── teaching/                                            │   │
│  │   │   ├── teaching1.md                                     │   │
│  │   │   └── config.yml                                       │   │
│  │   └── interests/                                           │   │
│  │       ├── interests1.md                                    │   │
│  │       └── config.yml                                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 用户交互流程

```
开始
  │
  ├─→ 点击导航栏 "EVENTS"
  │        ↓
  │   events.html (列表页)
  │        ├─→ ContentManager 扫描 contents/events/ 文件夹
  │        ├─→ 加载所有 events*.md 文件
  │        ├─→ 解析 YAML front matter 获取标题和日期
  │        ├─→ 按日期倒序排列
  │        └─→ 渲染为可点击的列表项
  │        ↓
  │   用户点击列表中的某个条目
  │   （例如：欢迎进入Polimi实验室）
  │        ↓
  │   events-detail.html?id=events1 (详情页)
  │        ├─→ 获取 URL 参数中的 ID (events1)
  │        ├─→ ContentManager 加载 contents/events/events1.md
  │        ├─→ 移除 front matter
  │        ├─→ 使用 marked.js 解析 Markdown
  │        ├─→ 使用 MathJax 渲染数学公式
  │        ├─→ 显示上一篇/下一篇导航按钮
  │        └─→ 渲染完整内容
  │        ↓
  │   用户可以：
  │   • 阅读详细内容
  │   • 点击"上一篇"/"下一篇"切换条目
  │   • 点击"返回"回到列表页
  │
  └─→ 结束
```

---

## 🔄 数据流

```
创建 markdown 文件
    ↓
    events1.md
    ┌──────────────────────────┐
    │ ---                      │
    │ title: 事件标题          │
    │ date: 2026-01-15         │
    │ ---                      │
    │ # 事件内容               │
    │ ...                      │
    └──────────────────────────┘
    ↓
ContentManager 初始化
    ├─→ fetch('contents/events/events1.md')
    ├─→ 使用正则表达式解析 front matter
    ├─→ 提取 title 和 date
    └─→ 存储完整内容
    ↓
列表页面渲染
    ├─→ 遍历所有文件
    ├─→ 生成 HTML: <a href="events-detail.html?id=events1">事件标题</a>
    └─→ 按 date 倒序排列
    ↓
用户点击链接
    ↓
详情页面加载
    ├─→ 获取 URL 参数 id
    ├─→ 从缓存中获取文件内容
    ├─→ 移除 front matter
    ├─→ marked.parse() 转换为 HTML
    ├─→ MathJax.typeset() 渲染公式
    └─→ innerHTML = HTML
```

---

## 📦 文件依赖关系

```
HTML 页面
├── events.html
│   ├─→ static/js/content-manager.js
│   ├─→ static/js/marked.min.js
│   ├─→ static/css/styles.css
│   └─→ static/css/main.css
│
├── events-detail.html
│   ├─→ static/js/content-manager.js
│   ├─→ static/js/marked.min.js
│   ├─→ static/js/navbar.js
│   └─→ MathJax CDN (数学公式)
│
├── teaching.html
│   └─→ 同 events.html
│
├── teaching-detail.html
│   └─→ 同 events-detail.html
│
├── interests.html
│   └─→ 同 events.html
│
└── interests-detail.html
    └─→ 同 events-detail.html


核心库
├── content-manager.js
│   ├─→ marked.js (Markdown 解析)
│   └─→ 浏览器原生 Fetch API
│
└── marked.js (CDN)
    └─→ 支持多种 Markdown 扩展


内容源
├── contents/events/*.md
├── contents/teaching/*.md
└── contents/interests/*.md
```

---

## 🔧 ContentManager 的主要方法

```javascript
┌─────────────────────────────────────────┐
│        ContentManager 类                  │
├─────────────────────────────────────────┤
│                                          │
│  new ContentManager('events')            │
│    ↓                                     │
│  • this.section = 'events'               │
│  • this.contentDir = 'contents/events/'  │
│  • this.files = []                       │
│                                          │
├─ async init()                            │
│  └─ 初始化，加载配置和发现文件            │
│                                          │
├─ async discoverFiles()                   │
│  ├─ 扫描 events1.md ... events10.md      │
│  ├─ 提取元数据                           │
│  └─ 按日期排序                           │
│                                          │
├─ extractMetadata(content)                │
│  └─ 解析 YAML front matter               │
│                                          │
├─ renderList(containerId)                 │
│  ├─ 生成列表项 HTML                      │
│  └─ 插入到 DOM                           │
│                                          │
├─ async renderDetail(containerId, id)     │
│  ├─ 获取文件内容                         │
│  ├─ 移除 front matter                    │
│  ├─ 使用 marked.parse() 渲染              │
│  └─ 触发 MathJax 重新渲染                │
│                                          │
├─ getFiles()                              │
│  └─ 返回所有文件数组                     │
│                                          │
└─ getFileById(id)                         │
   └─ 返回指定 ID 的文件                   │
```

---

## 🌍 URL 路由结构

```
网站根目录
│
├─ events.html
│   └─ URL: /events.html
│       └─ 显示: Events 列表
│
├─ events-detail.html
│   └─ URL: /events-detail.html?id=events1
│       └─ 显示: Events 详情 (events1.md)
│   └─ URL: /events-detail.html?id=events2
│       └─ 显示: Events 详情 (events2.md)
│
├─ teaching.html
│   └─ URL: /teaching.html
│       └─ 显示: Teaching 列表
│
├─ teaching-detail.html
│   └─ URL: /teaching-detail.html?id=teaching1
│       └─ 显示: Teaching 详情
│
├─ interests.html
│   └─ URL: /interests.html
│       └─ 显示: Interests 列表
│
└─ interests-detail.html
    └─ URL: /interests-detail.html?id=interests1
        └─ 显示: Interests 详情
```

---

## ✨ 关键特性

| 特性 | 描述 | 实现方式 |
|------|------|--------|
| **自动发现** | 自动扫描 markdown 文件 | for 循环 + fetch + 错误处理 |
| **元数据解析** | 提取标题和日期 | 正则表达式解析 YAML |
| **动态渲染** | 根据 URL 参数加载内容 | URLSearchParams API |
| **排序** | 按日期倒序 | Array.sort() |
| **导航** | 上一篇/下一篇 | 数组索引查找 |
| **Markdown 渲染** | 将 Markdown 转换为 HTML | marked.js |
| **公式渲染** | LaTeX 数学公式支持 | MathJax 3 |
| **响应式设计** | 移动端和桌面端适配 | Bootstrap 5 |

---

## 🎯 扩展性考虑

```
现在支持:
├─ events (events1.md ~ events10.md)
├─ teaching (teaching1.md ~ teaching10.md)
└─ interests (interests1.md ~ interests10.md)

要扩展到 50 个文件？
└─ 修改 content-manager.js 中的 maxFiles = 50

要添加新模块（如 projects）？
└─ 创建 contents/projects/
└─ 创建 projects.html
└─ 创建 projects-detail.html
└─ 新模块自动可用，无需修改引擎代码

要修改样式？
└─ 编辑 .content-item CSS 类

要改变排序逻辑？
└─ 修改 discoverFiles() 中的 sort 函数
```

---

希望这份架构图帮助您更深入地理解系统运作机制！🚀
